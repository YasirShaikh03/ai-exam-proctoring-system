# ─────────────────────────────────────────────────────────────────────────────
#  AI Exam Proctoring System — Elite Edition
#  Requirements File
#  GitHub: https://github.com/YasirShaikh03
# ─────────────────────────────────────────────────────────────────────────────

# Core Computer Vision
opencv-python>=4.5.0

# AI Face Landmark & Pose Detection
mediapipe>=0.10.0

# Numerical Computing
numpy>=1.21.0

# ─────────────────────────────────────────────────────────────────────────────
#  NOTE: The following are part of Python standard library (no install needed):
#    time, json, csv, os, math, struct, wave,
#    threading, collections, urllib.request, sys, datetime
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
#  NOTE: YOLOv4-tiny model files (cfg, weights, names) are downloaded
#  automatically on first run via urllib. No extra pip install needed.
# ─────────────────────────────────────────────────────────────────────────────

# Optional — Linux audio playback (install via system, not pip)
#   sudo apt install alsa-utils        ← for aplay (Linux)
#   macOS uses built-in afplay (no install needed)
#   Windows uses built-in winsound (no install needed)
